services:
  medical-summary-api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "$API_PORT:8000"  # Uses port from .env
    environment:
      # API envs
      HOST: 0.0.0.0
      PORT: 8000
      ENVIRONMENT: local