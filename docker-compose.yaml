services:
  medical-summary-api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${API_PORT:-8000}:8000"  # Uses port from .env
    environment:
      # API envs
      API_HOST: 0.0.0.0
      API_PORT: 8000
      ENVIRONMENT: local